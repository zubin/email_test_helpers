require 'spec_helper'

describe EmailTestHelpers do
  it 'has a version number' do
    expect(EmailTestHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
